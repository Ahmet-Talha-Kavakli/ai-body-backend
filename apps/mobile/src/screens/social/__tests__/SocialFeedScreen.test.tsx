import { describe, it, expect } from 'vitest'
import { SocialFeedScreen } from '../SocialFeedScreen'

describe('SocialFeedScreen', () => {
  it('should render social feed screen', () => {
    const component = <SocialFeedScreen />

    expect(component).toBeDefined()
  })

  it('should display friends shared meals', () => {
    const component = <SocialFeedScreen />

    expect(component).toBeDefined()
  })

  it('should show meal photos in feed', () => {
    const component = <SocialFeedScreen />

    expect(component).toBeDefined()
  })

  it('should allow liking shared meals', () => {
    const component = <SocialFeedScreen />

    expect(component).toBeDefined()
  })

  it('should support meal detail navigation', () => {
    const component = <SocialFeedScreen />

    expect(component).toBeDefined()
  })
})
