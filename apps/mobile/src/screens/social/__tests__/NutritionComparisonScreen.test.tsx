import { describe, it, expect } from 'vitest'
import { NutritionComparisonScreen } from '../NutritionComparisonScreen'

describe('NutritionComparisonScreen', () => {
  it('should render comparison screen', () => {
    const component = <NutritionComparisonScreen />

    expect(component).toBeDefined()
  })

  it('should display user nutrition', () => {
    const component = <NutritionComparisonScreen />

    expect(component).toBeDefined()
  })

  it('should compare with team average', () => {
    const component = <NutritionComparisonScreen />

    expect(component).toBeDefined()
  })

  it('should show macro differences', () => {
    const component = <NutritionComparisonScreen />

    expect(component).toBeDefined()
  })

  it('should display trending meals', () => {
    const component = <NutritionComparisonScreen />

    expect(component).toBeDefined()
  })
})
