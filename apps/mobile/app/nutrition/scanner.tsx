import { Text, View } from 'react-native'

export default function NutritionScanner() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#0A0A0F',
      }}
    >
      <Text style={{ color: '#fff', fontSize: 18 }}>Barkod tarayıcı yakında</Text>
    </View>
  )
}
