'use client'
// Gravity component - physics simulation placeholder
export function Gravity({ children }: { children?: React.ReactNode }) {
  return <>{children}</>
}
export function MatterBody({ children }: { children?: React.ReactNode }) {
  return <>{children}</>
}
import React from 'react'
export type GravityRef = Record<string, never>
